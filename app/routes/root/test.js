// const assert = require('assert')
// const controller = require('./controller')

describe('controller', () => {
  describe('method', () => {
    it('should work', (done) => {
      done()
    })
  })
})
