const config = {
  express: {
    port: 3000
  },
  mongodb: 'mongodb://localhost:27017/database',
  auth: false
}

// const env = require('../.env.json')
// console.log(env)

module.exports = config
